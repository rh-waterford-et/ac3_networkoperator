# configmap-manager-rolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: configmap-manager-binding
  namespace: sk1  # Replace with the correct namespace
subjects:
- kind: ServiceAccount
  name: ac3network-controller-sa  # Replace with your service account name
  namespace: sk1  # Ensure the namespace matches
roleRef:
  kind: Role
  name: configmap-manager
  apiGroup: rbac.authorization.k8s.io
